# 2026-06-23 工作日志

**2026-06-23 00:04 仪表板卡片交互增强 + in-progress 节点闪烁动画**:

### [模块:F8] 仪表板卡片点击显示详情
- **问题**: 顶部 7 个 KPI 卡片只能看汇总数值，无法了解统计值的详细构成
- **修复**: 
  1. 每个卡片添加 `onclick="openCardDetail('cardId')"`
  2. 新建 `DASH_DETAILS` 数据对象，包含 7 类卡片的细分内容（测试用例/通过率/工具/模型/评分/模式/问题）
  3. 复用右侧面板显示卡片详情：工具列表、模型提供商清单、评分维度条、版本历史柱状图等
  4. 右侧面板支持双模式切换：`panelMode = 'module' | 'card'`，关闭时自动恢复
- **状态**: ✅ resolved

### [模块:F8] In-Progress 节点边框脉冲动画
- **问题**: 分支优化节点 `b1a` (Web↔Desktop 前端合并) 为 `in-progress` 蓝色状态，但没有可见的闪烁/动画效果
- **修复**: 
  1. 新增 CSS keyframes `rectPulse` — stroke-opacity 在 1 ↔ 0.35、stroke-width 在 2 ↔ 2.8 之间交替
  2. `.node-rect.in-progress` 类绑定 `animation: rectPulse 1.6s ease-in-out infinite`
  3. 同时保留原有 `current-glow` 和 `current-blink` 动画（glow 过滤器 + 徽章闪烁）
- **状态**: ✅ resolved

### 修改文件
- `docs/pipeline/index.html`: CSS 动画 + 卡片 onclick + DASH_DETAILS 数据 + openCardDetail 函数 + 面板双模式

**2026-06-23 00:10 详情列表项可展开显示子内容**:

### [模块:F8] 详情面板列表项无展开功能
- **问题**: 点击卡片详情面板中的列表项（如"多模型适配器 127 tests"）没有反馈，看不到子类细分内容
- **修复**:
  1. 添加 CSS 样式：`.expandable` 可点击行、`.chevron` 箭头旋转动画、`.sub-list` 折叠/展开子列表
  2. 为所有 `DASH_DETAILS` 中的列表项添加 `expandItems` 子数据：
     - tests: 13 个模块各自的测试细分（按提供商/组件拆分）
     - pass: 5 个阶段的各模块测试数
     - tools: 15 个工具分组的具体工具名称和说明
     - models: 10 家提供的具体模型名称
     - modes: 4 种模式的功能点列表
  3. 新增 `toggleExpand(uid, event)` 函数处理点击展开/折叠
  4. 渲染时检查每个 item 是否有 `expandItems`，有则渲染为可展开行
- **状态**: ✅ resolved

### 修改文件
- `docs/pipeline/index.html`: CSS 可展开样式 + DASH_DETAILS 子数据 + toggleExpand 函数 + 渲染逻辑改造
