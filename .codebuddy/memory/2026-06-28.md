# Memory 2026-06-28.md

## release-publish.bat execution (00:13:46)
- Version: v0.6.10
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## 发版 push 竞态条件根治 (00:25-00:28)
- 问题：release-publish.bat 每次 git push origin main 都被 rejected (non-fast-forward)
- 根因：发版构建期间（3-5分钟），CI pipeline sync 推了管线数据 commit 到 main
- 第二层问题：git hooks(post-commit)自动运行 unified-sync 修改 docs/pipeline/*.json，导致 pull --rebase 失败
- 修复：scripts/release.mjs 和 release-publish.bat 两处的 push 前加 `git stash → git pull --rebase origin main → git stash pop`
- 安全：远程 CI 只改 pipeline JSON，本地只改 version/CHANGELOG，文件集不相交，永无冲突
- 文档：docs/47_发版推送竞态条件分析与根治.md

## release-publish.bat execution (00:28:06)
- Version: v0.6.12
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## CI sync-pipeline 竞态修复 (00:34)
- CI workflow (ci.yml) 的 sync-pipeline job 同样因缺 pull --rebase 而 push rejected
- 修复：.github/workflows/ci.yml 的 Commit pipeline data 步骤 push 前加 git pull --rebase
- CI 不需要 stash（ubuntu-latest 无本地 hooks 干扰管线文件）
- 三处全部修复：scripts/release.mjs + release-publish.bat + .github/workflows/ci.yml
- 文档同步更新：docs/47_发版推送竞态条件分析与根治.md 新增位置3修复、竞态全景图

## release-publish.bat execution (00:40:58)
- Version: v0.6.13
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## CI sync-pipeline unstaged 残留修复 (00:49)
- 问题：CI sync-pipeline 报 `cannot pull with rebase: You have unstaged changes`
- 根因：git add 只列出 5 个管线文件名，但 unified-sync 实际生成更多文件（_issue_cache.json 等）
- 修复：git add docs/pipeline/（整个目录）+ 保留 git stash → rebase → pop 保护链
- CI 其实也有 hooks 干扰管线文件（ubuntu-latest 的 post-commit 也跑 unified-sync），故 stash 必要
- v0.6.13 的 Release workflow 还有 Build Desktop (GH_TOKEN) 和 Build Web (jsx-runtime) 两个独立问题待查

## Release workflow Build Desktop + Build Web 修复 (00:51)
- Build Desktop: electron-builder 的 package.json build.publish.provider="github" 导致尝试发布，缺 GH_TOKEN 报错
  修复：release.yml Package 步骤加 env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
- Build Web: packages/web/package.json 缺 react/react-dom 依赖，Vite/Rollup 无法从 frontend 源码解析 react/jsx-runtime
  修复：packages/web/package.json dependencies 加 react/react-dom
- 四个截图全部修复完毕

## 复盘文档 (00:53)
- 创建 docs/48_v0.6.13_发版CI全链路修复深度复盘.md
- 涵盖 4 个截图错误的完整根因分析、修复方案、经验教训 + 快速排查手册
- 关联 doc 47（push 竞态），形成发版 CI 全链路认知体系

## release-publish.bat execution (00:55:02)
- Version: v0.6.14
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## release-publish.bat execution (00:57:24)
- Version: v0.6.15
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## GitHub Release EXE 大文件清理 (00:57)
- 删除 v0.6.12 和 v0.6.13 的 EXE + blockmap 文件，释放约 220 MB 空间
- v0.6.14 和 v0.6.15 为保留版本（14版本之后）
- 命令: gh release delete-asset -R ht182400-creator/easyagent -y

## CI Sync Pipeline Data rebase 冲突根治 (10:32)
- 问题：发版时两个 commit 几乎同时推送 → 两个 CI sync job 并行 unified-sync → pull --rebase merge conflict
- v0.6.14 CI 验证：Release 全绿（Build Desktop/Build Web 修复生效），CI sync-pipeline-data 因冲突失败
- 根治方案：ci.yml Commit pipeline data 步骤改为冲突时 rebase --abort → reset --hard origin/main → 重跑 unified-sync → commit → push
- 核心原理：管线文件是全量生成的，reset 后重跑 unified-sync 得到基于最新远程 + 本轮测试报告的完整数据，无数据丢失

## D方案：CI/CD可复用Workflow优化 (12:07)
- 问题：每次发版 3 个 workflow（2 CI + 1 Release），CI 为 artifacts commit 重复跑 8 个 job
- 方案：提取测试 job 为 `_test.yml`（workflow_call），ci.yml + release.yml 共同引用
- 改动：新建 `_test.yml`（7 job），重写 `ci.yml`（删测试 job，引用 _test.yml），`release.yml`（加 tests 质量门禁 + sync-pipeline），`scripts/release.mjs`/`release-publish.bat`（commit 加 [skip ci]）
- 效果：发版 workflow 3→1，测试配置唯一真相来源，质量门禁（测试不通过不构建）
- 日常代码同步（push main 不发版）：零影响，ci.yml 触发机制不变
- 文档：docs/49_CI_CD工作流优化_D方案_可复用Workflow.md
- 验证：YAML结构 × 工作流引用 × job依赖链 × artifact一致性 × [skip ci]标记 × lint检查 → 全部通过

## Rebase 冲突破解 + Push (16:45-17:00)
- 问题：`git pull --rebase` 在 pick `c5338ce`（v0.6.16）时发生冲突，4 个管线文件时间戳冲突
- 解决：HEAD 时间戳更新（02:37 > 02:32），以 HEAD 为准；2 小文件手动编辑，2 大文件 `checkout --ours`
- 额外问题：post-commit hook 每次 commit 后重新生成管线数据，形成 commit→脏文件→commit 死循环，用临时 rename hook 截断
- 最终 push：`219891a..35a3428 main -> main`，本地与远程完全同步
- 下一步：观察 GitHub Actions CI workflow 是否正常运行（D方案验证关键）

## GitHub Release EXE 大文件清理 (17:20)
- 删除 v0.6.14 和 v0.6.15 的 EXE + blockmap 文件，释放约 210 MB 空间
- v0.6.13 和 v0.6.12 此前已清理
- 当前无任何版本有可下载的 EXE，等 v0.6.17 发版后恢复

## release-publish.bat execution (17:21:38)
- Version: v0.6.17
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## Release workflow 自动触发修复 (17:27)
- 问题：v0.6.17 发布后 release.yml 没有自动触发，需手动 workflow_dispatch
- 根因：scripts/release.mjs 用 `git push origin main --follow-tags` 一次性推送 commit（含 [skip ci]）+ tag，GitHub 把整次 push 事件全跳过
- 修复：分两次 push — 先 push commit（[skip ci] 抑制 ci.yml）、再单独 push tag（触发 release.yml）
- 同时加 `git restore docs/pipeline/` 回退 post-commit hook 产生的管线文件修改，避免 rebase 冲突
- Commit: cba41d8 fix: release.mjs 分两次 push，避免 [skip ci] 抑制 tag 触发的 release.yml
- 文档: docs/49 新增第 8 节"实战场验证：v0.6.17 发布问题与修复"，含根因分析、修复方案、时序图、经验教训
- Commit: d18c2da docs: D方案 v0.6.17 实战场验证与修复记录

## eslint.config.cjs scoped 包路径修复 (17:50)
- 问题：CI Lint job 报 `pnpmRequire: 找不到包 @eslint/js`（被 `continue-on-error: true` 掩盖，不阻塞）
- 根因：`pnpmRequire` 中 `pkgName.split('/').pop()` 对 scoped 包 `@eslint/js` 只返回 `js`，丢失 `@eslint` scope 目录
- 修复：改为直接使用 `pkgName`（如 `@eslint/js`），`path.join` 自动处理跨平台分隔符
- 文件：packages/frontend/eslint.config.cjs 第 26 行

## [skip ci] 双重抑制问题 + release.mjs 修复 (18:03)
- 问题：v0.6.18 发版 tag push 后 release.yml 没有自动触发
- 根因：GitHub Actions 的 `[skip ci]` 不仅跳过 commit push 的工作流，也跳过该 commit 所在 tag push 的工作流（因为 tag 指向的 commit 含 `[skip ci]`）
- v0.6.17 验证：之前的手动触发 workflow_dispatch 成功就是因为 tag push 被 `[skip ci]` 抑制了，根本不存在 "分两次 push 能绕过" 的情况
- 修复：scripts/release.mjs 去除 commit message 中的 `[skip ci]`（第 356 行），ci.yml 重复跑一次无害，release.yml 正常触发才关键
- v0.6.18 Release：手动 workflow_dispatch 触发 → https://github.com/ht182400-creator/easyagent/actions/runs/28318774622
- 副作用：ci.yml 会在发版 commit 上多跑一次（约 5 分钟），可接受

## v0.6.18 双问题复盘文档 (18:12)
- 创建 docs/50_v0.6.18_发版双问题复盘_eslint路径与skipCI抑制.md
- 涵盖：问题① eslint.config.cjs pnpmRequire scoped 包路径解析错误 → 问题② [skip ci] 双重抑制 Release workflow
- 包含：根因分析、修复方案、全流程时间线、Rebase 冲突处理、Post-commit Hook 干扰、经验教训
- MEMORY.md 补充文档链接

## v0.6.19 Release workflow 双问题修复 (18:23)
- 问题① eslint.config.cjs 的 pnpmRequire 在 CI 仍然失败（"找不到包 @eslint/js"）
  - 根因：v0.6.18 的 `.pop()` 修复不够——pnpmRequire 的 .pnpm 目录扫描本身在 CI 环境不可靠
  - CI 上 `.pnpm` 目录前缀可能因版本/peer dep 差异而不匹配 `startsWith('@eslint+js@')`
  - 根治：双策略回退——① 先尝试 root createRequire（public-hoist-pattern 环境）② 回退到 .pnpm 扫描（严格隔离模式）
  - 文件：packages/frontend/eslint.config.cjs 完全重写 pnpmRequire 函数
- 问题② release.yml Sync Pipeline Data 报 "not currently on a branch"
  - 根因：tag push 触发 release → actions/checkout 检出 tag → detached HEAD → git push 失败
  - 修复：checkout tag 后加 `git checkout main` 切换到 main 分支
  - 文件：.github/workflows/release.yml sync-pipeline job 新增 Switch to main branch 步骤
- Release workflow 自动触发已生效 ✅（v0.6.19 是首次 push tag 自动触发 release.yml）
- 副作用：ci.yml 在发版 commit 上多跑一次（5 分钟），可接受

## release-publish.bat execution (18:12:11)
- Version: v0.6.19
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## v0.6.20 release-publish.bat rebase 冲突修复 + 根因根治 (18:43-18:48)
- release-publish.bat Step 3 中 `git pull --rebase origin main` 因 4 个 pipeline 文件冲突失败
- **真根因**：release.mjs 使用 `git add .`，将 post-commit hook 遗留的脏管线文件纳入 commit
  - post-commit hook → update-progress.mjs → unified-sync.mjs --force 修改管线 JSON
  - 这些脏文件留在工作区，下次 `git add .` 时被暂存
  - release commit 包含管线数据 ≠ origin/main（CI auto-sync 推送的）→ rebase 冲突
- **根治**：`git add .` 前先 `git checkout HEAD -- docs/pipeline/` 恢复管线文件到 HEAD 状态
  - commit 后再次 `git checkout HEAD -- docs/pipeline/` 清理 post-commit hook 新产生的修改
  - 文件：scripts/release.mjs 第 359-381 行
- 冲突解决：`git checkout --ours` → `git rebase --continue` → `git tag -f v0.6.20 HEAD`
- push main + tag 成功 ✅，Release workflow 自动触发（push 事件）
- Steps 3.5-6 未执行，需重新运行 `release-publish.bat` 选 `[0] skip` 继续

## 50 文档更新 — 问题③ 复盘完成 (18:51)
- 在 `docs/50_v0.6.18_发版双问题复盘_eslint路径与skipCI抑制.md` 新增第七章
- 内容覆盖：
  - 7.1 错误表现（4 管线文件 CONFLICT）
  - 7.2 表象 vs 根因
  - 7.3 完整调用链分析（脏文件产生 → 被吞入 commit → `git restore` 为何无效）
  - 7.4 设计层面问题分析（架构/工具/Hook三层缺陷，两个 Writer 写同一组文件）
  - 7.5 修复方案（`git checkout HEAD -- docs/pipeline/` 在 `git add .` 之前）
  - 7.6 修复效果验证
  - 7.7 架构级根治方案（`.gitignore` + artifact 等三种方案）
  - 7.8 v0.6.20 冲突应急解决过程
- 同时更新了标题（三问题）、TOC、第五章根因表、第六章工作流全景图、变更记录

## 清理 v0.6.20 之前 GitHub Release 中的 EXE 大文件 (19:01)
- 通过 `gh api` DELETE 删除 3 个 Release (v0.6.17/18/19) 的 6 个资产
- 每个 Release 删除: EasyAgent-x.x.x-win-x64.exe (104MB) + .blockmap
- 保留轻量资产: latest.yml, web-dist.tar.gz
- 释放约 314 MB GitHub Release 存储空间
- 方法: `gh api repos/.../releases/assets/{id} -X DELETE`（`gh release delete-asset` 因 tag 匹配问题失败）

## CI 双问题修复 (19:10)
- **问题1 (release.yml sync-pipeline)**: `git checkout main` 在 tag 触发的 workflow 中失败 — 因为 actions/checkout@v4 检出 tag 后进入 detached HEAD，本地无 main 分支引用
  - 修复: checkout 步骤加 `ref: main`，直接从 main 分支检出
  - 文件: `.github/workflows/release.yml` 第 388-393 行
- **问题2 (lint-and-format @eslint/js 缺失)**: `eslint.config.cjs` 需要的 `@eslint/js`, `eslint`, `typescript-eslint` 从未在 package.json 中声明
  - 修复: 添加三包到 root devDependencies（`.npmrc` 已有 hoisting 配置，仅缺依赖声明）
  - 文件: `package.json` + `pnpm-lock.yaml`
- 本地验证: `node -e "require('./packages/frontend/eslint.config.cjs')"` ✅

## 50 文档更新 — 问题④⑤ 复盘完成 (19:11)
- 在 `docs/50_v0.6.18_发版双问题复盘_eslint路径与skipCI抑制.md` 新增第八、九章
- 问题④：release.yml sync-pipeline `git checkout main` 失败 — actions/checkout@v4 在 tag push 事件下检出 tag → detached HEAD → 无 main 分支引用
  - 修复：checkout 步骤加 `ref: main`
- 问题⑤：eslint 核心依赖从未在 package.json 中声明 — eslint/@eslint/js/typescript-eslint 未写入 devDependencies → CI 不安装
  - 修复：root devDependencies 添加三包
- 联动更新：标题（五问题）、TOC、1.1 问题全景表、1.2 关联分析、1.3 涉及文件、5.1 关联图谱、第五章根因表、第六章新增 6.7+6.8、2.5 交叉引用第九章
- 五个问题覆盖两条触发链：链A (release.mjs 发版流程) 问题②③，链B (eslint配置+CI环境+release.yml) 问题①④⑤

## release-publish.bat execution (19:00:07)
- Version: v0.6.20
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## release-publish.bat execution (19:28:27)
- Version: v0.6.21
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## v0.6.21 CI 全部失败根因与修复 (19:51)
- 问题：CI 和 Release workflow 所有 job 的 "Install dependencies" 步骤失败
- 根因：`pnpm-lock.yaml` 记录的 eslint specifier 为 `^9.39.4` / `^8.62.0`，但 `package.json` devDependencies 写的是 `^9.15.0` / `^8.15.0` → `pnpm install --frozen-lockfile` 拒绝安装
- 真因：之前添加 eslint 依赖时手动写了最小版本 `^9.15.0`，但 `pnpm install` 解析后 lockfile 记录了实际最高版本。package.json 和 lockfile 不一致
- 修复：`package.json` 三包版本号与 lockfile 对齐（^9.15.0→^9.39.4, ^8.15.0→^8.62.0），CI 恢复通过

## release-publish.bat 管线文件自动清理 (19:44)
- 需求：发版后管线文件总是脏的（post-commit hook 残留），每次需要手动 checkout，容易忘记
- 修复：release-publish.bat 两处添加自动清理：
  1. Step 7 artifacts commit 前 `git checkout HEAD -- docs/pipeline/`（防止脏管线混入 artifacts commit）
  2. :FINAL_DONE 末尾 `git checkout HEAD -- docs/pipeline/`（最终兜底，全流程结束后用户看到干净工作区）
- 与 scripts/release.mjs:363 的清理形成三层保护链

## 修复 51 处 no-empty lint 错误 (20:05)
- 问题：12 个测试文件中 `catch (err) {}` / `catch (_) {}` 空块导致 CI lint 报 51 个 error
- 原因：测试清理 `rmSync` 的 catch 块为空 —— 清理失败本就不应影响测试结果，但缺少注释违反 `no-empty` 规则
- 修复：`} catch (err) {}` → `} catch (_) { /* 测试清理失败不影响结果 */ }`
- 使用脚本 `temp/fix-empty-catch.mjs` 批量处理，验证 `no-empty count: 0`
- 文件：config-manager(10), project-tools(7), session-manager(6), preview-media-tools(6), subagent-db-tools(5), knowledge-tools(5), code-tools(4), file-extra-tools(4), builtin-tools(1), git-advanced-tools(1), file-tools-security(1), mcp-client(1)

## release-publish.bat execution (20:13:39)
- Version: v0.6.22
- DTS check + EXE build + GitHub Release + Pipeline Sync completed

## GitHub Release EXE 大文件清理 — v0.6.21 (20:12)
- 需求：删除 0.6.22 之前版本的安装包 EXE 大文件
- GitHub 现状：10 个 Release (v0.6.12 ~ v0.6.22)，只有 v0.6.21 含 EXE (105.4 MB)
  - v0.6.20 及更早版本无 EXE（仅 latest.yml + web-dist.tar.gz）
- 删除操作：
  - v0.6.21: EasyAgent-0.6.21-win-x64.exe (105.4 MB) + .blockmap (0.1 MB) → 释放 105.5 MB
- 保留：所有版本的 latest.yml（autoupdate 清单）、web-dist.tar.gz（Web 部署包）
- 工具：temp/cleanup-old-releases.mjs (DELETE /repos/{repo}/releases/assets/{id})
- 验证：v0.6.22 唯一保留 EXE (105.4 MB)，v0.6.21 仅剩 latest.yml

## LangGraph 工作流引擎 — Phase 1-3 完成 (21:03 ~ 21:26)
- 需求：用 LangGraph 全面重写 Agent 工作流引擎
- 目录：packages/langgraph/ — 独立包，复用现有 core 接口
- 产出：
  - **文档 4 份**：00_项目规划.md, 01_整体架构.md, 02_约束规范.md, 03_开发路线图.md
  - **核心代码 10 文件**：
    - state/AgentState.ts — Annotation API 声明状态（messages/turnCount/maxTurns/token 用量等 12 个字段）
    - nodes/thinkNode.ts — LLM 思考节点（SystemMessage + 历史消息 → LLM → AIMessage）
    - nodes/actNode.ts — 工具执行节点（并行/串行工具调用 + 超时控制）
    - nodes/observeNode.ts — 观察节点（结果处理 + 轮次检查）
    - edges/routeAfterThink.ts — 条件路由（tool_calls → act, 否则 → END）
    - graph/agentGraph.ts — StateGraph 编译（START→think→route→act→observe→think 环形）
    - memory/Checkpointer.ts — SQLite Checkpoint 持久化（BaseCheckpointSaver 接口，rowid 排序）
    - memory/Memory.ts — 长期记忆管理（压缩摘要 + 偏好提取 + 关键词搜索）
    - Agent.ts — 主类（run/stream/resume/getState/listSessions 兼容 AgentEngine）
    - index.ts — 公共导出
  - **测试 2 文件，14 测试全通过**：
    - agent.test.ts — 7 测试（纯文本/工具循环/轮次限制/空消息/错误处理/并行工具/工具失败）
    - persistence.test.ts — 7 测试（SQLite CRUD/线程隔离/MemorySaver 集成）
- 架构：环形路径 START→think→routeAfterThink→(act→observe→think | END)
- 设计决策：
  - 接口最小化：thinkNode/actNode 使用独立类型定义而非直接依赖 core 包
  - Checkpointer 使用 rowid 排序（避免 created_at 同秒歧义）
  - MemorySaver 用于测试集成；SqliteCheckpointer 待完善 LangGraph v0.2 BaseCheckpointSaver 接口兼容后集成
  - 工具执行默认并行（Promise.all），支持 forceSequential 模式
- 待实施：Phase 4（并行工具依赖分析），Phase 5（多 Agent 协作子图）

## LangGraph 测试完善 & Checkpointer 修复 (21:29 ~ 21:35)
- 新增 `__tests__/agent-class.test.ts` — 10 个测试覆盖 LangGraphAgent 主类（run/resume/stream/abort/getState/listSessions/clearHistory）
- **关键修复**：
  - `SqliteCheckpointer` 从 `implements BaseCheckpointSaver` 改为 `extends BaseCheckpointSaver` — 基类是 abstract class，`implements` 不会继承 `getNextVersion` 方法，导致 LangGraph PregelLoop 在 `loop.js:242` 调用 `.bind()` 时报 `undefined`
  - 所有方法改为 async（`getTuple`/`list`/`put`/`putWrites` 返回 Promise）
  - `getLatestState` 从原始 checkpoint JSON 中提取 `channel_values`，解决 resume 时状态格式不匹配问题
  - `list` 方法改为 `async *list`（async generator），移除内部 `asyncGeneratorFromArray` 辅助函数
- 最终测试结果：**3 文件 24 测试全部通过**

## LangGraph 测试文档编写 (21:35)
- 新建 `packages/langgraph/docs/04_测试文档.md` — 完整的测试说明文档
- 内容覆盖：
  - 测试架构总览（3层模型: 图级→Agent级→持久化级）
  - 运行命令速查
  - Mock 组件工厂详解（createMockChat / createMockToolExecutor / buildAgent）
  - 全部 24 个测试用例详细说明（含表格、流程图、代码示例）
  - 如何添加新测试（图级/Agent级/持久化级模板）
  - 已知限制（Mock非真实LLM、流式测试限制、Checkpoint格式差异、extends vs implements、async要求）
  - 测试 Roadmap（Phase 1-8）

## LangGraph 调试日志系统 (21:59)
- 新建 `src/logger/Logger.ts` — 完整调试日志基础设施
  - 五级日志：DEBUG / INFO / WARN / ERROR / SILENT
  - 模块化子 Logger（每个源文件一个实例）
  - 结构化输出：时间戳 + 等级 + 模块名 + 消息 + JSON 数据
  - 性能计时器 (startTimer/endTimer)
  - 环境变量配置 (LANGGRAPH_LOG_LEVEL / LANGGRAPH_LOG_FILE / LANGGRAPH_LOG_MODULES)
  - 可选文件输出 + 模块白名单过滤
- 集成到全部 8 个源文件：
  - Agent.ts — 构造/run/resume/stream/abort/clearHistory/close 全生命周期日志
  - agentGraph.ts — 编译流程 + 节点创建计时
  - thinkNode.ts — LLM 调用耗时/token 用量/tool_calls
  - actNode.ts — 工具调用列表/并行模式/每工具耗时/结果摘要
  - observeNode.ts — shouldContinue 决策
  - routeAfterThink.ts — 路由决策（三种原因）
  - Checkpointer.ts — DB 操作计时（getTuple/put/putWrites/listThreads）
  - Memory.ts — 压缩/提取/搜索/记忆管理
- 更新 `docs/04_测试文档.md` — 追加第十一章"调试日志系统"，含使用指南、模块覆盖表、环境变量说明
- index.ts 导出 Logger API 供外部使用

## LangGraph 日志配置：环境变量 → 配置文件改造 (22:18)
- 用户反馈：环境变量不稳定，要求改用配置文件
- 新建 `src/logger/LogConfig.ts` — 配置加载器
  - `loadLogConfig(path?)` — 从 JSON 配置文件加载（自动发现或指定路径）
  - `setupLogConfig(config)` — 纯代码配置，无需文件
  - `saveDefaultConfig(path?)` — 生成模板配置文件
  - 发现顺序：显式路径 → 自动发现(cwd向上5级) → 环境变量兜底 → 内置默认值
- 配置文件格式：`langgraph.config.json` 的 `logger` 段，含 level/file/modules/console 四字段
- 创建示例 `langgraph.config.json` 于 packages/langgraph/
- `setupFromEnv` 标记为 @deprecated，保留作为兜底兼容
- 更新导出：src/logger/index.ts、src/index.ts 新增 loadLogConfig/setupLogConfig/saveDefaultConfig
- 更新文档：`docs/04_测试文档.md` 11.4 节重写为配置文件优先
- 验证：tsc --noEmit 通过，48 测试全部通过

## LangGraph Demo 启动慢优化 (22:31)
- **根因：tsx 不在项目依赖中**，`npx tsx` 每次触发网络解析/下载（首次 10-30s），加上 tsx 需实时编译 LangChain 重型依赖
- 修复：`tsx: "^4.19.0"` 加入 packages/langgraph 的 devDependencies
- start-demo.bat：`npx tsx` → `pnpm demo`（调用 package.json scripts 中的本地 tsx）
- package.json 新增 `"demo": "tsx scripts/run-demo.ts"` 脚本
- tsup.config.ts 保持简洁（回退了多入口方案）
- tsc --noEmit 验证通过

## LangGraph Demo Web UI — 有向图可视化面板 (22:40)
- 用户反馈：PowerShell 终端中看不清晰，要求页面内渲染有向图 + 节点卡片展开
- 新建 `scripts/demo-server.ts` — HTTP 服务器 (Node.js 内置 http 模块，端口 3456)
  - `GET /api/graph` — 图结构 JSON（6 节点 + 6 边）
  - `GET /api/scenarios` — 6 个场景元数据
  - `POST /api/run/:id` — 运行单个场景并返回 JSON 结果
  - `POST /api/run-all` — 批量运行全部场景
  - 启动时自动打开浏览器
- 新建 `demo/index.html` — 暗色终端风格可视化面板
  - 顶部 SVG 有向图渲染（节点高亮 + 边动画 + 循环边流动动画）
  - 6 张场景卡片网格（入场动画、hover 效果）
  - 点击卡片展开 → 终端风格日志输出 + 结果摘要（轮次/消息数/耗时）
  - "执行全部场景"按钮一键运行
  - 设计：深色背景 (#080c12) + 霓虹青/绿/琥珀色 + JetBrains Mono 字体
- start-demo.bat 支持 `--web` 模式
  - `start-demo.bat` → 终端模式
  - `start-demo.bat --web` → Web UI 模式
- package.json 新增 `"demo:web": "tsx scripts/demo-server.ts"`
- tsc --noEmit 验证通过
- 新建 `scripts/run-demo.ts` — 独立可执行 Demo 脚本，6 个场景演示有向图流转
  - 脱离 vitest，使用 npx tsx 直接运行 TS 源码
  - Mock 工厂（mockChat/mockExec）纯 JS 实现，零测试框架依赖
  - ANSI 颜色输出，结构清晰
- 新建 `start-demo.bat` — 一键启动批处理
  - Step 1: 检查 langgraph.config.json 配置
  - Step 2: 自动构建（dist/ 不存在时），支持 --skip-build 跳过
  - Step 3: 运行 npx tsx scripts/run-demo.ts
- 验证：npx tsx scripts/run-demo.ts 6 场景全部通过

## start-demo.bat Unicode 制表符乱码修复 (22:49)
- 用户执行 `start-demo.bat --web` 报错：`'�═══════╝' is not recognized as a command`
- 根因：Windows CMD 对 UTF-8 制表符（╔╗╚╝║）解析不稳定，即使 chcp 65001 仍可能乱码，导致部分字符被截断为"命令"
- 修复：所有制表符→ASCII `=`，emoji（❌✅💡）→文本标签（[FAIL]/[OK]/Usage:）
- 此为 Windows bat 文件通用陷阱：不要使用 Unicode box-drawing 字符

## 新手文档: 05_启动与执行指南_新手向.md (22:52)
- 用户要求将测试启动、批处理使用方法、内部实现写成文档供新手使用
- 新建 `packages/langgraph/docs/05_启动与执行指南_新手向.md`（约 350 行）
- 覆盖十章：快速开始、启动模式（终端/Web UI）、等效 pnpm 命令、config 说明、核心文件结构、内部实现详解（start-demo.bat / run-demo.ts / demo-server.ts / index.html 流程图）、有向图速查、FAQ（8 个）、编码注意事项、相关文档索引
- 同步更新 00_项目规划.md 目录结构，加入 scripts/、demo/、start-demo.bat 及新文档入口

## start-demo.bat 二次修复: type|findstr 管道乱码 (22:53)
- 用户再次执行 `start-demo.bat --web` 仍报 `'raph.config.json' is not recognized`（lang 被吃掉）
- 根因：`type langgraph.config.json | findstr "level"` 中 pipe `|` 在 chcp 65001 下不稳定，type 输出多字节字符时 CMD 管道错误截断
- 修复：`type X | findstr Y` → `findstr /c:"Y" X > nul 2> nul`（findstr 直接读文件，去掉 pipe）
- 此为 Windows bat 第二陷阱：chcp 65001 + type + pipe 组合不可靠

## start-demo.bat 第三次修复: chcp 65001 + 中文 echo 导致解析器失序 (22:55)
- 用户执行后报 `... was unexpected at this time.` — CMD 经典解析错误
- 根因：chcp 65001（UTF-8 代码页）是 CMD 公认 bug——echo 中文后解析器对后续字符错位，把 `...` 误读为命令标记
- 修复：① 删除 `chcp 65001`（中文 Windows 默认 936 即可正常显示中文）② 所有 echo 中的 `...` 删掉
- Windows bat 三号陷阱总结：
  1) 禁用 Unicode 制表符（╔╗╚╝║）
  2) 禁用 type|findstr 管道 + UTF-8，用 findstr /c:"X" file 直接读
  3) 禁用 chcp 65001，中文 echo 后会导致后续行解析错乱

## MEMORY.md 新增 bat 文件编写铁律章节 (22:58)
- 用户要求将 Windows bat 三大陷阱写入 MEMORY.md 作为永久参考
- 在 `关键陷阱清单` 与 `Web↔Desktop 代码隔离约束` 之间新增 `## ⚠️ Windows bat 文件编写铁律` 章节
- 三元禁令表格 + 额外注意事项（:::注释/括号转义/编码推荐）
- 此后每次写 .bat 文件前应查阅此节，避免重复踩坑

## start-demo.bat 全面英文化 + 图可视化字体优化 (23:04)
- 用户反馈：① 中文乱码持续（`鏈夊悜鍥惧彲瑙嗗寲`）② 箭头文字太小不清晰 ③ 底图解释被压住
- **bat 文件修复**：所有 echo 中文 → 英文（如 `检查配置文件`→`Checking config file`），彻底消除编码问题
- **index.html 图可视化修复**：
  - `edge-label`：font-size 9px→12px，fill→#8899aa，新增 stroke 描边背景（白色字描黑边，边缘线上也清晰）
  - `node-desc`：font-size 8px→10px，fill→#667788（原来未渲染，现 JS 中补上 `<text class="node-desc">` 元素）
  - SVG viewBox：`0 50 800 420`→`0 30 800 450`（上下各多 20-30px 空间）
  - 容器高度：440px→500px
  - 箭头 marker：8x6→10x7（大 25%）
- bat 铁律增补：**优先用全英文写 bat**，消除一切编码猜测

## 图可视化文字堆叠修复 — desc 从外部移入矩形内部 (23:07)
- 用户截图指出红框区域：node.desc 与相邻边标签（addEdge）重叠
- **根因**：desc 文字放在 `node.y + node.h + 16`（矩形外侧下方），与从节点底部出发的边标签位置冲突
- **修复**：
  1. 所有节点增高：START/END h=36→50，process/decision h=42→56
  2. desc 移入矩形内部：label 居中偏上（cy-5），desc 居中偏下（cy+11）
  3. desc CSS 新增 `dominant-baseline: central` 确保垂直居中
  4. SVG viewBox 调整：`0 30 800 450`→`0 20 800 470`（适配更高节点）
- **设计原则**：节点标签文字应全部在矩形内部，外侧只留给边/边标签

## 边标签被节点压住修复 (23:11)
- 用户截图：`循`（来自 `循环` 边标签 observe→think）被 route 节点遮住
- **根因**：边标签 Y 坐标用 `(fromP.y + toP.y) / 2`，以下两类情况标签落入了节点矩形内：
  1. 循环边 observe→think：标签 Y 在 think 节点内部
  2. 水平边 route→act/END：标签 Y=296 在 route 矩形 (280-336) 内
- **修复**：
  1. 循环边标签：`labelY = min(fromP.y, toP.y) - 30`（放到 think 上方 30px）
  2. 水平边标签（左↔右）：`labelY = min(fromNode.y, toNode.y) - 6`（放到节点顶部上方 6px）
- **关键教训**：边标签计算必须引用 fromNode/toNode 的实际 Y 边界，而不是端口 Y 坐标

## 图布局优化：observe 上移形成倒 T 型布局 (23:16)
- 用户建议将 observe 放到 think 同高度左侧，act 在 observe 下方
- **改造**：
  - observe: (140,380)→(180,130) 与 think 同高，act: y=240 在 observe 下方
  - 删除 special observe→think 曲线处理代码（现在只是简单水平 right→left 连线）
  - 删除 special 循环边标签位移代码（统一归入水平边分支）
  - viewBox: `0 20 800 470`→`0 10 800 360`（布局更紧凑）
- **效果**：
  - observe→think：原来是左下→右上的复杂曲线，现在变成一条水平虚线，清晰直观
  - act→observe：垂直向上连线，简单干净
  - 整体呈倒 T 型：主流程（START→think→route）居中，左路（act/observe）和右路（END）对称展开
  - 减少了一个方向类别（原来是左下→右上跨象限曲线，现在是标准水平线）

## checkpoint 序列化导致 getType() 报错修复 (23:26)
- 错误：`state.messages[0]?.getType is not a function` 在 thinkNode 第149行
- **根因**：LangGraph 的 SqliteCheckpointer 使用 JSON.stringify/parse 保存/恢复 State，messages 数组中的 BaseMessage 实例（AIMessage/HumanMessage 等）变成**普通 JS 对象**，丢失了 prototype 上的 getType() 方法
- 影响范围（共 4 个文件，9 处调用）：
  - `thinkNode.ts`：toChatMessages(3处) + 轮次终止判断 + SystemMessage 判断
  - `actNode.ts`：lastMessage.getType() + getType 日志
  - `observeNode.ts`：msg.getType() === 'tool'
  - `routeAfterThink.ts`：instanceof AIMessage + lastMessage?.getType()
- **修复**：
  1. 新建 `src/graph/messageUtils.ts` — 统一的消息类型判断工具
     - `getMessageType(msg)`：优先调 getType()，否则读 .type 属性，再兜底 constructor.name
     - `hasToolCalls(msg)`：安全读取 tool_calls
     - `getToolCallId(msg)`、`getMessageContent(msg)`：兼容两种格式
  2. 所有 `msg.getType()` → `getMessageType(msg)`
  3. `toChatMessages` 重写：用 hasToolCalls/getToolCallId 替代 'tool_calls' in msg 检查
  4. `routeAfterThink`：`instanceof AIMessage` → `getMessageType() === 'ai' && hasToolCalls()`
- **关键教训**：任何使用 LangGraph checkpoint 的系统，消息处理代码必须兼容"类实例"和"普通对象"两种格式
