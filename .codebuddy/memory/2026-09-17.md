# 2026-09-17 开发日志

## [审核:整体] 专家团八视角最终审核（架构/流程/代码/UI/测试/分析/运维/产品）

- **问题**: 需对项目做最终审核，给出改进意见与方案（含"模型是否最新""UI 是否用 three.js""是否重构"）
- **修复**: 交付 `docs/62_专家团最终审核报告.md`；本次只做审核与结论，未改动业务代码
- **状态**: ✅ resolved

### 审核方法（关键：实测取代引用）
- 全量代码勘察（12 个 workspace 包）+ 关键源码精读 + 配置核查
- **实测管线数据**：读 6 份 `_vitest-*.json` 逐条统计失败用例，而非引用文档声称值
- 外部对标 2026-09 前沿模型与 Agent 协议栈（MCP/A2A/AG-UI）

### 核心发现（12 条可复现缺陷，详见 `docs/62` 附录 A）

| ID | 发现 | 证据 |
|----|------|------|
| D1 🔴 | **公网 REST API 完全无鉴权** | `Authorization` 仅出现在调用上游 LLM 处；WS 的 `EASYAGENT_WS_TOKEN` 为"未设则不校验" |
| D2 🟠 | **30 个孤儿测试** | desktop 30 失败全部指向已迁到 `frontend/src/components/layout/` 的旧组件测试（B1a 合并后未清理） |
| D3 🟠 | **LangGraph resume 真实失败** | `agent-class.test.ts` → `resume 同一会话应该恢复到之前状态` |
| D4 🟠 | **Tailwind 令牌引用断裂** | `--surface-sidebar`/`--text-primary`/`--border-subtle` 在前端 CSS 中 **0 命中**，但 14 个组件在用；被 `body` 颜色继承兜底掩盖 |
| D5 🟠 | **测试数字 6 处矛盾** | 1273 / 1195 / 1514 / 实测 1260 已执行·31 失败·83% 覆盖率·`_stale: true` |
| D6–D12 🟡 | 签名校验关闭 / embeddingModel 未用 / 无 DB 迁移 / Markdown 属性逃逸 / vscode 空壳 / MCP 协议 2024-11-05 / CI 与 dmg-AppImage 不一致 | 见报告 |

### 实测基线（修正了长期被引用的错误数字）
- 定义用例 **1514**（48 文件 / 30 模块）；vitest 已执行 **1260**，通过 1229，**失败 31**，覆盖率 83%
- `pipeline-data.json` 标记 `_stale: true`（最新 vitest 报告为 2026-07-01）
- 单文件规模：`server/src/index.ts` **3759 行**、`docs/pipeline/index.html` **2201 行**、`frontend/pages` 中 **9 个**超 500 行（DocsGuide 1003）

### 评分卡
文档 92 / CI 85 / 架构 78 / 运维 72 / UI 70 / 代码 68 / 产品 65 / 测试 62 / **安全 45** → **综合 72 (B-)**

### 三个点名问题的结论
1. **模型升级**：要更新（OpenAI 预设仍 `gpt-4o` 落后两代；缺 Anthropic/Google；`reasoning_content` 未解析），但**重点是机制**——远程目录唯一真源 + 季度刷新 + 下线预警
2. **three.js**：**明确不建议用于主 UI**。仅当图谱 >2000 节点且 SVG 实测掉帧时，用 `sigma.js`（非裸 three.js）局部替换并保留降级。优先级远高的替代投入：`monaco` > `shiki` > `markdown-it` > `xterm`
3. **重构**：重构 3 处（ContextManager / `server index.ts` 拆包 / 知识库向量化）；**不碰 5 处**（monorepo 结构、双引擎、技术栈代际大版本、组件库、`core` 的 ts+js 共存）

### P0 行动项（约 6.5 人日）
1. REST 加 Bearer 鉴权 + 限流 + 默认只监听 `127.0.0.1`（0.5d）
2. 清理 30 个孤儿测试 + 修 `LangGraphAgent.resume`（1d）
3. 单一数据源 + CI 一致性门禁（1d）
4. 抽出 `ContextManager` MVP（工具 schema 分级 + 结果截断 + 滚动摘要 + token 预算）（3d）
5. 修 Tailwind 令牌引用 + 加 CI 变量校验（0.5d）
6. 刷新 vitest 报告并重跑 `unified-sync.mjs`（0.5d）

---

## [记忆:MEMORY] MEMORY.md 归档重写为精简版 v2.0

- **问题**: `MEMORY.md` 达 712 行 / 77KB，超出会话注入上限被截断，且内含过期数据（"1195 tests / 100% 通过"）
- **修复**: 就地重写为约 360 行结构化精简版 —— 保留全部 52 条陷阱清单 + 核心规则 + bat 铁律 + 服务器部署事实；把管线 v2.1 细节改为指针式引用（`docs/pipeline/ARCHITECTURE.md`）；更正测试数字为实测值
- **状态**: ✅ resolved

---

## 流程记录：本次审核使用的临时脚本

- `temp/_review-stats.mjs` —— 统计前端页面/关键文件行数 + 测试映射 + 管线 KPI
- `temp/_review-vitest.mjs` —— 逐条汇总 6 份 `_vitest-*.json` 的失败用例（定位孤儿测试的关键工具）
- 两脚本位于同步排除目录 `temp/`，已在 `docs/62` 附录 A 引用

<!-- 记忆维护说明：本项目每日日志为追加式，严禁覆盖既有内容；MEMORY.md 可就地更新。 -->
