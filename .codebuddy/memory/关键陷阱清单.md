# 关键陷阱清单（MEMORY.md 附录）

> 从 `MEMORY.md` 外置（2026-09-19）：主文件控制体积以便完整注入，本附录按需读取。
> 关联：`docs/14_构建前必检清单.md`、`docs/修复汇总.md`（详表）、`packages/desktop/scripts/verify-build.cjs`（门禁）。

## A. 代码/打包陷阱 57 条

> 范围标记：🖥️=Desktop 专属 / 🌐=Web 专属 / 🔀=两者影响。**新 bug 修复后必须在此加一行 + 在 `verify-build.cjs` 加检查**。

| # | 🎯 | 陷阱 | 修复 |
|---|-----|------|------|
| 1 | 🔀 | electron-builder v24 意外升级 → NSIS EnVar 缺失，exe 仅 0.3MB | 精确锁 23.6.0，删 v24 残留 |
| 2 | 🔀 | Vite 5 `<style>` 内联 → `No matching HTML proxy` | 改外部 CSS link |
| 3 | 🖥️ | `localhost` 解析成 IPv6 → Dashboard 全 `--` | 全改 `127.0.0.1` |
| 4 | 🖥️ | 双重 `.json()` → 数据不显示且错误被吞 | `apiFetch<T>(url).then(d => ...)` |
| 5 | 🔀 | CSS `@import` 不在首行 → Tailwind 失效 | 移到最顶部 |
| 6 | 🔀 | pnpm symlink → asar 里找不到 `@easyagent/core` | tsup `noExternal` bundle |
| 7 | 🔀 | Express 子依赖遗漏 → `Cannot find module 'body-parser'` | 显式声明全部子/孙依赖 |
| 8 | 🖥️ | VS Code watcher 锁定 → 删 app.asar `Access Denied` | watcherExclude + taskkill |
| 9 | 🖥️ | better-sqlite3 原生编译失败 | 预编译 `.node` + `npmRebuild:false` |
| 10 | 🔀 | `packages/server/src/index.js` 残留 → vitest 加载旧 JS | 删除旧编译产物 |
| 11 | 🖥️ | apiFetch 双重 `.json()` 全项目 13 文件 43+ 处 | 去掉 `res.json()`，直接用返回值 |
| 12 | 🔀 | bat `[!]` + 延迟扩展冲突 → 整行解析崩溃 | 改 `[^^!]` |
| 13 | 🔀 | bat 中文乱码（CMD 936 vs UTF-8） | JSON 数据本身正确，仅显示层；见 §C |
| 13a | 🔀 | git status 中文名显示 octal 转义 | `git config --global core.quotepath false` |
| 14 | 🔀 | bat `:::` 注释被当非法 label → CMD 崩溃 | 全部改 `rem` |
| 14a | 🔀 | CMD `if(...)` 块内 echo 含 `)` → 块提前关闭（`^)` 不可靠） | 改 `goto` 标签模式 |
| 15 | 🔀 | `execSync('node ' + path)` 路径含空格被截断 | 加双引号 `node "${path}"` |
| 16 | 🔀 | `.mjs` 含 TS 类型注解 → SyntaxError | 纯 JS |
| 17 | 🔀 | esbuild 0.20.1 对 catch 语法脆弱 → `Expected "finally"` | 统一 `catch (err)`；verify #9 拦截裸 `catch {}` |
| 18 | 🔀 | PowerShell `Set-Content` 默认 ANSI → 76 个中文文件被毁 | **只用 Node `writeFileSync(..., 'utf8')`**；verify #14 检测 |
| 19 | 🔀 | pnpm v11 `allowBuilds` 占位文本被当 false | 显式设 `true` |
| 20 | 🖥️ | asar 内加载 better-sqlite3 失败 | `!node_modules/better-sqlite3/**` 排除出 asar + `extraResources` |
| 21 | 🖥️ | `mime` 缺失 → Release 下 Express 500 | desktop 显式加 `"mime": "^1.6.0"`（**不是 2.x**）；verify #11 |
| 22 | 🖥️ | 开发/Release better-sqlite3 MODULE_VERSION 不一致（137 vs 123） | `npx node-gyp rebuild --target=30.0.0 --arch=x64 --dist-url=https://electronjs.org/headers --release`；verify #10 + build.bat Phase 2.5 |
| 23 | 🖥️ | electron-updater 传递依赖缺失（dev 可用/Release 崩） | desktop 显式加 8 个传递依赖；verify #12 |
| 24 | 🖥️ | Express 生态版本不兼容（iconv-lite/media-typer/ipaddr.js/encodeurl） | 保持监控；verify #13 WARN |
| 25 | 🖥️ | apiFetch 双重 `.json()` → TypeError 被静默吞 | 用 `apiFetch<T>` 泛型 |
| 26 | 🖥️ | HashRouter 下 `<a href>` → 黑屏 | 用 `<Link>` / `navigate()`；verify #15 |
| 27 | 🖥️ | asar 内 `PROJECT_ROOT` 只读 → 知识库写入 400 | `createApp({ projectRoot })`，Desktop 传 `homedir()` |
| 28 | 🔀 | CI `windows-latest` 升级 VS 2026 → node-gyp 不识别，0 jobs | 全部固定 `windows-2022` |
| 29 | 🖥️ | Desktop Tailwind `content` 漏 frontend → 布局类丢失 | 加 `'../frontend/src/**/*.{js,ts,jsx,tsx}'` |
| 30 | 🖥️ | `pnpm exec @electron/rebuild` bin 名歧义 + 不生效 | 改 `npx --yes node-gyp rebuild ...`；按文件大小判断 |
| 31 | 🖥️ | tsup 内联 server → asar 里两份 CORS，旧 main.js 覆盖新逻辑 | 改 server 后**同时** `tsup --clean` 两个包；asar 修补要同时替换两处 |
| 32 | 🖥️ | pnpm hardlink 下 node-gyp rebuild 假成功（exit 0 但文件未变） | 重建后查 mtime+大小+头值；verify Phase 2.5 |
| 33 | 🖥️ | electron-rebuild 在 pnpm 下静默跳过 | 直接用 `node-gyp` 显式参数 |
| 34 | 🖥️ | 5 个脚本争抢 `better_sqlite3.node` → MODULE_VERSION 反复变 | 精简为 2 个：`rebuild-sqlite3.mjs`（唯一编译入口）+ `sqlite3-loader.mjs`（运行时切换） |
| 35 | 🖥️ | 字节扫描 MODULE_VERSION 假阳性（始终 116） | 勿扫字节；用 SHA256 比对 + `rebuild-sqlite3.mjs --verify` |
| 36 | 🖥️ | build.bat sqlite3 路径基于 CWD → 误判"不存在"→ 每次多余 rebuild | 路径加 `%~dp0` 前缀 |
| 37 | 🖥️ | Desktop 独立 renderer CSS 与 frontend 两套 | ✅ 已删，统一由 `frontend/main.tsx` import |
| 38 | 🔀 | pnpm isolated 下 `pnpm exec eslint` 找不到 bin | 用 `scripts/lint.bat` / `clean.bat` 直调 `.pnpm` 路径 |
| 39 | 🖥️ | electron-builder 找不到传递依赖（pnpm isolated 无 `.pnpm/`） | tsup `noExternal` 内联 express/cors/ws/multer；pino 例外须保持 external |
| 40 | 🖥️ | LangGraph checkpoint 序列化丢 BaseMessage 原型方法（`getType is not a function`） | 统一用 `getMessageType(msg)`；`toChatMessages()` 用 hasToolCalls/getToolCallId |
| 41 | 🔀 | LangGraph 普通聊天误暴露 benchmark 工具 → 死循环 `Recursion limit reached` | 系统提示词约束；过滤 `benchmark_*`；`consecutiveFailures>3` 停止；`recursionLimit = maxTurns*3+10` |
| 42 | 🔀 | 小模型被 66 个工具 schema 污染（占上下文 19-25%）→ 语气偏移/输出 JSON 解释 | 按模型分级暴露工具（7B→15-20 个；70B+→完整） |
| 43 | 🌐 | 插件市场安装进度卡"准备中"（WS 未建立） | HTTP 轮询兜底 1s/次 ≤60s；终态清理 |
| 44 | 🌐 | 已安装插件仍显示"使用"（id 前缀 `local:` 不匹配） | 按 id + name 双匹配 |
| 45 | 🔀 | `fetch failed` 与 `Failed to fetch` 是两种字符串 | 错误匹配兼容两者 |
| 46 | 🔀 | Ollama 未启动 → 只显示 "fetch failed"，原因不明 | 区分后端宕机 vs 上游 LLM 不可达；给出"请运行 ollama serve" |
| 47 | 🔀 | 插件 `execute` 返回 string 而非 `ToolResult` → LLM 无意义反思循环 | 四层防御：模板修正 + `normalizePluginResult()` + actNode 兜底 + 可识别错误前缀 |
| 48 | 🔀 | LLM 非流式 → thinkNode 重复同一工具 → 160s 死循环 | 连续相同工具调用 ≥2 次即停；`consecutiveIdenticalToolCalls` |
| 49 | 🌐 | `tool_use` WS 事件早于 assistant 占位消息 → 工具卡片丢失 | handler 中自动补建占位消息 |
| 50 | 🌐 | toolCallId 不匹配（伪造两个不同 ID）→ 工具结果永远丢 | 用 LangChain `event.run_id` 作 toolCallId |
| 51 | 🌐 | Doc_project 面板集成 | Server 静态托管 `/doc-viewer/`（vite `base:'/doc-viewer/'`），`X-Frame-Options: SAMEORIGIN`，WS `open_panel` |
| 52 | 🌐 | 双缓存导致插件装到旧版本 | `RELEASE_CACHE_TTL=5min` + `skipCache` 层层穿透 + `POST /api/plugins/market/refresh` |
| 53 | 🔀 | **「检查的路径」≠「使用的路径」→ 假通过**（2026-09-19 评测链路）：dry-run 只查 `core/dist/index.js` 存在，真评测却导 `dist/benchmark/BenchmarkRunner.js`（tsup 无该 entry → **永不存在**）；同时 dry-run 用自备宽松解析器读数据集，与评测的加载器不同源 → 「环境检查通过（10 题）」与「真跑 0 题 / 核心包未编译」长期并存 | 校验逻辑必须**调用与生产同一条代码路径**（真实 `import` + 真实引擎读数据），失败必须给**非 0 退出码**。同族：`verify` 输出被过滤成空白当通过（§9.3）、组件类名不存在不报错（陷阱 #5x）。详见 `docs/77` |
| 54 | 🧹 | **生成物留在仓库内会污染"扫描类"工具**（2026-09-19）：评测真实执行把 `solution.test.ts` 写进 `temp/benchmark-run/`，被 `docs/pipeline/scripts/scan-test-cases.mjs` 计入定义用例（实测 **+21 条**）→ `_stale` 门禁变红 | 双保险：① 扫描器 `EXCLUDE_DIRS` 补 `temp`/`logs`/`release`/`benchmark-results`/`.obsidian`/`.codebuddy` ② 生成方默认自我清理（**通过即清、失败保留**）。判据：**凡"被 gitignore 的产物目录"，必须同时从扫描类工具的遍历范围排除** |

| 55 | 🧪 | **"只在并行下失败"的间歇用例**（2026-09-19 实测复现）：core 并行 **17 轮中 1 轮出现 1 个文件失败（≈6%）**，同日串行多轮 0 失败；2026-09-18 亦曾有 11 条同类失败（集中在插件/沙箱全局状态类用例） | 根因未定位前**不要**把 `fileParallelism` 设为默认（`packages/core/vitest.config.ts` 现为**环境变量开关**：`pnpm test:core:fast` = `EASYAGENT_CORE_PARALLEL=1`，且不写管线 JSON）。定位法：反复快跑，**失败时保留该轮完整日志**（`--reporter=default` 会打印失败文件名与用例名），先锁定文件，再判断"共享资源冲突 vs 超时过紧" |
| 56 | ⏱️ | **性能优化前先补基准（本轮两次凭直觉都错了）**：① 以为评测用例的嵌套 vitest 占 20s，实测仅 **6.46s**；真正的第一名是 `exec-tools-security` **7.6s**（它的"放行"用例真的 spawn 了 `npm install lodash` / `git push origin main`）② 曾用"并发跑测试"得出"33 文件 / 928 用例"的假象（缺的正是新增的 10 个文件） | 一律先量：`vitest run --reporter=json --outputFile=...` 后按 `endTime - startTime` 排序取 Top；**测量期间不要跑任何其它 vitest**（并发会互相污染时长与结果） |

| 57 | 🐌 | **CLI 旗标会让 vitest 静默不跑用例**（2026-09-19 踩到）：给嵌套 `vitest run` 加 `--maxWorkers=1`，**用时从 1672ms 降到 935ms 且退出码 0**，但 JSON 报告是 `total=0` 空报告 —— 判定链路会因此退化成"恒未通过"（正是 `docs/77` 记录的假信号家族）。同族：`--fileParallelism --maxWorkers 4` 组合时值被当成"测试名过滤" → `no tests` | **判定与测速一律看报告内容，不能只看退出码与墙钟**；"变快了"先怀疑"是不是没跑"。已验证无效/有害的旗标要写进代码注释（见 `SolutionRunner` 的五项参数对照）。另：`--no-isolate` 在同场景**无收益**（1779ms vs 1672ms），也别加 |

## B. 环境陷阱（本仓库特有）

| 陷阱 | 现象 | 解法 |
|------|------|------|
| **pnpm 安装被 IDE 批量删除保护拦截** | `ERR_PNPM_LINKING_FAILED` / `SAFE_DELETE_BULK_CONFIRM_REQUIRED`，count 501 > 500；依赖进了 store 但**没链接到包**，且 `package.json` 未更新；计数 scope 是 turn，**重试不归零** | 手动写 `package.json` + `pnpm install`；必要时对**该条命令**设 `CODEBUDDY_SAFE_DELETE_ENABLED=0`，跑完立即 `Remove-Item Env:CODEBUDDY_SAFE_DELETE_ENABLED` |
| **中断的 install 会静默破坏其他包链接** | desktop 包 0 用例，报 `Failed to resolve "@testing-library/jest-dom"`，而前端测试全绿 | 跑**全量** `pnpm install` 修复。**改依赖后必须跑全量回归** —— 只看改动所在的包会漏掉跨包链接损伤 |
| **DOMPurify 在 happy-dom 下不可靠** | 连 `<p>`/`<h2>`/`<span class>` 都被整类剥掉，多元素时 `<script>` 反而可能存活 → 安全断言给出**错误结论** | 测试文件加 `// @vitest-environment jsdom`。本仓库默认 happy-dom（v0.6.22 为规避 React 重复实例），但**不渲染 React 的测试文件**可安全切 jsdom |
| **引用不存在的定义不报错** | `prose prose-invert`、Tailwind 令牌指向未定义 CSS 变量 —— 都是静默失效 | 用脚本门禁兜底：`verify-css-tokens.mjs`；新引入第三方类名前先确认依赖已安装 |
| **vitest 别名挡住原生 better-sqlite3** | core 的 vitest 把 `better-sqlite3` 别名到内存 mock → 依赖真实 SQLite 语义的代码拿不到 user_version/事务回滚 | 专项测试用 `createRequire(import.meta.url)('better-sqlite3')` **绕过 Vite 别名**；迁移器对 mock 环境自动跳过（读到版本为 null） |
| **语义分析「静默 0」（cwd 陷阱）** | `/api/semantic/*` 未传 path 时曾退回 `process.cwd()`（Electron 下是安装目录甚至 System32）→ 扫 0 文件却返回 `success:true` + 全 0 统计，用户无法自查 | 默认目录必须用**注入的 `projectRoot`**（Web=仓库根 / Desktop=宿主传入）；显式 path 非法回 **400**；空结果 UI 回显「本次扫描根」（`routes/semantic.ts` 的 `resolveWorkspace`）。F14 先例 |
| **语义扫描会被日志/文档灌满** | `logs/**` 未被忽略 + `maxFiles` 是硬上限 → 名额被非代码文件占满，实测**本仓库 typescript 文件数 = 0** | `IGNORE_PATTERNS` 已含 `logs`/`release`；`prioritizeSourceFiles()` 保证代码文件优先（新增语义逻辑前先看该函数）。F28 先例 |
| **格式债务（prettier）** | 252 个文件不符合 → CI Format check 只能 `continue-on-error`，门禁形同虚设 | **2026-09-19 已清零**：提交前跑 `pnpm format`，保持 `prettier --check "packages/**/*.{ts,tsx,json,css}" "scripts/**/*.mjs" "docs/**/*.md"` 全绿 |
| **3000 端口已被自建 Forgejo 占用** | EasyAgent 主服务 = **3456**、前端 Vite = 5173、LangGraph Demo = 3455；预览工具曾默认 3000 → EADDRINUSE；安装脚本也曾提示 3000（错） | **禁止把 3000 当默认端口**；用户项目预览用 **3500-3599 自动选空闲**；端口权威表 = `docs/52`。F15 先例 |
| **沙箱命令黑名单会误杀引号内字符** | 旧 `containsShellMetacharacters` 把引号内 `(` `)` 当子 shell → `node -e 'console.log(1+1)'` 被拒；且 exec 超时 kill 后 `if (killed) return` → Promise 永不 settle（请求挂死） | 已根治为**不经 shell**（`spawn(argv, {shell:false})` + `sandbox/commandSafety.ts` 引号感知解析）；改沙箱执行逻辑前先读该模块头注释。F17 先例 |
| **`checkDockerAvailability` 是一次性闩锁缓存** | `dockerChecked = true` 后**永久**返回旧结论（无 TTL）→ Docker 晚于后端启动可用时，沙箱卡在本地模式、UI「重新检测」无效 | 需重探先调 `resetDockerCache()`（已从 core 根入口导出）；`/api/sandbox/status` 已改为每次实探。F21 先例 |
| **`{ type:'信封', ...event }` 会被 event.type 覆盖** | WS 广播自动化进度时 `{ type:'automation_progress', ...event }` 而 event 带 `type:'agent_start'` → 前端只认信封类型 ⇒ 实时进度日志永远为空 | 信封字段必须放在展开**之后**（或另起 `step` 字段）；前端用 `normalizeAutomationStep()`（`step ?? type`）。F24 先例 |
| **`ask_user` 在自动化里无人应答** | 无人值守任务调用 `ask_user` 时旧实现只把问题当文本返回 | **已支持 HITL**：服务端注册「可交互版」`ask_user`（`ToolRegistry.register()` **按名覆盖**，两引擎通用）→ `server/src/hitl.ts` 的 `QuestionBroker` 广播 `agent_question`，用户界面作答；超时 5 分钟则自行决策。接口 `GET /api/agent/questions`、`POST /api/agent/answer`。F25 先例 |
| **前端缺 import 只有 tsc 能发现** | ESM 下用未导入的标识符（如 JSX 里 `<RefreshCw />` 忘 import）= 运行时 `ReferenceError` → **整页黑屏**；esbuild/vite 构建不检查 | 已固化门禁 `pnpm verify:types`（在 `verify:all` 的 types 项里，前端 0 错误）；CI `_test.yml` 的 test-desktop / build-check 有 `npx tsc --noEmit`。**vitest 不做类型检查，不能当门禁** |
| **测试写用户真实数据目录（自我毒化）** | ① `KnowledgeService.getGlobal()` 默认 `~/.easyagent/.easyagent/knowledge` → core/server 并行互污，用例失败后清理不执行 → **永久失败** ② `AutomationManager` 默认 `~/.easyagent/data/automations.json` → 每跑一次留 2 条任务，累积成「任务列表 100 条」 | 测试必须重定向：知识库 `EASYAGENT_GLOBAL_KB_DIR`、自动化 `EASYAGENT_AUTOMATIONS_FILE`（写临时文件）；`addDocument` 同名复用旧 id。F18 / F23 先例 |

## C. Windows bat 文件铁律

**三条禁令**（违反任一条 → CMD 报错信息与真实原因完全无关）：

| # | 禁令 | 替代 |
|---|------|------|
| 1 | 禁用 Unicode box-drawing 字符（`╔ ║ ⚠️` 等） | 用 ASCII `=====`、`[OK]`、`[FAIL]` |
| 2 | 禁用 `type file \| findstr`（chcp=65001 时管道截断） | `findstr /c:"kw" file > nul 2> nul` |
| 3 | 禁用 `chcp 65001`（CMD 公认 bug，echo 中文后解析器失序） | **不设 chcp**，中文 Windows 默认 936 |

**次要**: `:::` → `rem`；`if(...)` 块内 echo 含 `)` → 改 `goto`；文件存 UTF-8 without BOM。
