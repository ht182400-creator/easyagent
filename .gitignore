node_modules/
dist/
.vite/
*.db
*.db-journal
.env
.env.local
coverage/
test-results/
build-output.txt
tsconfig.tsbuildinfo
*.exe
*.zip
*.blockmap
*.tgz
# 压测诊断快照（数值随机器浮动，不入库）
benchmarks/load-latest.json
*.tar.gz
release/
desktop/release/
# IDE 用户配置（保留 .codebuddy/memory 共享）
.codebuddy/automations/
.codebuddy/teams/
# EasyAgent 本地数据
.easyagent/knowledge/
# 上下文工程：工具结果截断后的完整内容落盘（工作区内，供模型按需 read_file 取回）
.easyagent/context/
# 临时文件
.codebuddy/temp/
# 根目录调试产物目录（脚本/日志/解压包，防污染 git 状态 #14）
temp/
# Obsidian 第三方插件与配置（非本仓源码）
.obsidian/
**/.obsidian/
# 对话历史日志（可能含敏感信息，仅本地保留）
history_*.md
docs/history_*.md
# 临时测试/调试脚本
_test_*.mjs
docs/pipeline/_test_*.mjs
docs/pipeline/_test_*.json
docs/pipeline/_add_*.mjs
docs/pipeline/_check_*.mjs
docs/pipeline/_debug.mjs
docs/pipeline/_direct_test.mjs
docs/pipeline/_scan_issues.mjs
docs/pipeline/_vitest-*.json
packages/docs/
# GitHub Token（仅本地使用，勿提交）
scripts/.release_token

# 一次性评审产物（13MB JSON 报告；如需留存请放 docs/ 并压缩后再入库）
/专家团审核报告与架构改进方案_*.json
# 一次性缺陷/用例 JSON 报告（12MB 级，根目录 dump；如需留存请放 docs/ 并压缩后再入库）
/修复*.json

# vite 构建输出日志（误提交于 v0.6.43 前，已 git rm --cached 移除跟踪）
build_out.txt
# CI 回取的原始日志（仅本地保留）
docs/pipeline/ci-logs/
# 根目录调试产物（截图/日志/临时文件，防止再次堆积 #14）
*.png
$null
/NUL
18.0.0
# 根目录杂项文件（Obsidian/离线工具产物，不参与同步）
未命名.base
temp/root-cleanup/
# 测试日志：汇总（.log/.html/summary.json）作为项目资产入库；各包原始输出体积大，不入库
logs/test-logs/*/raw/
# 运行日志：每日轮转、量级大，不入库（需要留档请摘录到 docs/）
logs/runtime/
# 兜底：从包目录启动时若误建了包级 logs/，也不要入库
packages/*/logs/
# 构建/部署/启动等命令输出日志（logs/build-logs/），量级大，不入库
logs/build-logs/
# 评测产物（swebench_report_*.md + latest-summary.json；离线自测结果不代表模型能力，不入库。
# 需要留档请把摘要摘录进 docs/77 或 README 的评测表）
benchmark-results/
