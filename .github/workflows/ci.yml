name: CI Test

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Test windows-2022
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Echo
        run: echo "windows-2022 works!"
