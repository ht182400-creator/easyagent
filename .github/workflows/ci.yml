name: CI Test

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Quick Test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Echo test
        run: echo "CI on Windows is working!"
